# Deps cho backend chạy trong Docker — CHỈ dùng REMOTE encoder (Kaggle), nên
# KHÔNG cần torch/transformers/sentence-transformers/timm (chỉ dùng khi nạp model
# TẠI CHỖ — core/query_encoders.py import các gói này LAZY bên trong hàm, không ở
# module-level, nên bỏ khỏi image không lỗi import). Nhẹ hơn requirements.txt gốc
# rất nhiều -> build Docker nhanh, image nhỏ.
fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic==2.12.4
python-multipart==0.0.32
faiss-cpu
meilisearch
pyarrow
numpy==2.3.5
pandas==2.3.3
requests
anthropic
openai
google-generativeai
Pillow
